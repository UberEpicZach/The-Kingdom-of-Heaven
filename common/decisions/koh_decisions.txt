host_diet_decision = {
	picture = "gfx/interface/illustrations/decisions/decision_misc.dds"
	ai_check_interval = 60

	cost = { gold = standard_activity_cost }

	#cooldown = { days = standard_feast_cooldown_time }

	desc = host_feast_decision_opening_desc
	selection_tooltip = host_feast_decision_tooltip

	is_shown = {
		is_landed = yes
		highest_held_title_tier > tier_barony
		top_liege = {
			has_primary_title = title:e_hre
		}
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	effect = {
		title:d_mainz.holder.capital_province = {
			spawn_activity = {
				type = activity_diet
				owner = root
			}
		}
		custom_tooltip = host_feast_decision_desc_vassals
		if = {
			limit = {
				is_independent_ruler = no
			}
			custom_tooltip = host_feast_decision_desc_liege_opinion
		}
	}
	
	ai_potential = {
	}

	ai_will_do = {
	}
}

mount_market = {
	picture = "gfx/interface/illustrations/decisions/decision_misc.dds" #get a horse icon
	ai_check_interval = 60

	cost = { gold = standard_activity_cost }

	#cooldown = { days = standard_feast_cooldown_time }

	desc = host_feast_decision_opening_desc
	selection_tooltip = host_feast_decision_tooltip

	is_shown = {
		has_global_variable = crusadermounts_initialized
	}

	is_valid_showing_failures_only = {
		NOT = { 
			crusadermounts_is_inaccessible_or_incapable_trigger = yes 
			has_character_flag = visiting_mount_market
		}
	}

	is_valid = {
		NOT = { 
			crusadermounts_is_inaccessible_or_incapable_trigger = yes 
			has_character_flag = visiting_mount_market
		}

	}

	effect = {
		hidden_effect = {
			set_character_flag = visiting_mount_market
			trigger_event = csm.010 
		}
	}
}

mount_market = {
	picture = "gfx/interface/illustrations/decisions/decision_misc.dds" #get a horse icon
	ai_check_interval = 60

	cost = { gold = standard_activity_cost }

	#cooldown = { days = standard_feast_cooldown_time }

	desc = host_feast_decision_opening_desc
	selection_tooltip = host_feast_decision_tooltip

	is_shown = {
		has_global_variable = crusadermounts_initialized
	}

	is_valid_showing_failures_only = {
		NOT = { 
			crusadermounts_is_inaccessible_or_incapable_trigger = yes 
			has_character_flag = visiting_mount_market
		}
	}

	is_valid = {
		NOT = { 
			crusadermounts_is_inaccessible_or_incapable_trigger = yes 
			has_character_flag = visiting_mount_market
		}

	}

	effect = {
		hidden_effect = {
			set_character_flag = visiting_mount_market
			trigger_event = csm.010 
		}
	}
}