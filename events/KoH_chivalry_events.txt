namespace = chivalry

chivalry.1 = {
	hidden = yes
	trigger = {
		is_ai = no
		is_ruler = yes
	}
	
	immediate = {
		every_living_character = {
			set_variable = {
				name = Chivalry
				value = 10
			}
		}
	}
	
	option = {
		name = chivalry.1.a
	}
}