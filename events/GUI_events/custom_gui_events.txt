# Custom GUI and dummy character initialization events
# DO NOT EDIT THIS FILE DIRECTLY, MAKE YOUR OWN COPY INSTEAD

namespace = CustomGUI

# CustomGUI.1 initializes DummyMale and DummyFemale, which allows for use of effects like add_custom_gui and add_sidebar_button to save GUIs from script.
CustomGUI.1 = {
	type = character_event
	name = "Custom GUI"
	desc = "Initializing Custom GUI system, please wait..."
	widget = {
		gui = "dummy_character_start_widget" # Put your event_widget's name here. Note that it needs to be in a file with the same name as the actual gui name.
		container = "custom_widgets_container"
	}
	option = {
		name = "Did something break?"
	}
}
# CustomGUI.2 reads the list of everything that has been passed to add_custom_gui and creates a GUI with that name. It should be fired once all relevant add_custom_gui commands have been run.
CustomGUI.2 = {
	trigger = {
		NOT = {
			is_widget_open = custom_sidebar
		}
	}
	type = character_event
	name = "Custom GUI"
	desc = "Loading Custom GUI, please wait... If this window isn't closing due to a bug, you can manually initialize any missing GUIs by clicking the corresponding buttons below."
	widget = {
		gui = "custom_gui_start_widget"
		container = "custom_widgets_container"
	}
	option = {
		name = "Did something break?"
	}
}